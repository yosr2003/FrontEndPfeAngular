import { DossierSoinMedical } from './dossier-soin-medical';

describe('DossierSoinMedical', () => {
  it('should create an instance', () => {
    expect(new DossierSoinMedical()).toBeTruthy();
  });
});
