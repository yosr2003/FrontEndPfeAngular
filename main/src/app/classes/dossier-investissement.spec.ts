import { DossierInvestissement } from './dossier-investissement';

describe('DossierInvestissement', () => {
  it('should create an instance', () => {
    expect(new DossierInvestissement()).toBeTruthy();
  });
});
